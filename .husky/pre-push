#!/bin/sh

echo "🔍 Running pre-push release verification..."
bash ./scripts/verify-ci.sh || {
  echo "❌ verify:release failed. Fix before pushing."
  exit 1
}

echo "✅ verify:release passed. Pushing..."
